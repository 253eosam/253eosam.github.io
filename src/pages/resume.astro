---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import ResumeSection from '../components/resume/ResumeSection.astro';
import SkillTag from '../components/resume/SkillTag.astro';
import resume from '../data/resume';

const { profile, skills, experience, certifications, education, activities } = resume;

const splitLinkedText = (text: string, label: string) => {
  const index = text.indexOf(label);

  if (index === -1) {
    return null;
  }

  return {
    before: text.slice(0, index),
    after: text.slice(index + label.length),
  };
};
---

<BaseLayout title="이력서" description={`${profile.name} - ${profile.role}`}>
  <Header current="resume" />
  <main class="container resume">
    {/* ── 프로필 ── */}
    <section class="profile">
      <div class="profile__top">
        <div class="profile__text">
          <h1 class="profile__name">{profile.name}</h1>
          <p class="profile__role">{profile.role}</p>

          <div class="profile__contact">
            <a href={`mailto:${profile.email}`}>{profile.email}</a>
            {profile.links.map((link) => (
              <a href={link.url} target="_blank" rel="noopener noreferrer">{link.label}</a>
            ))}
          </div>
        </div>

        {profile.profileImage && (
          <img src={profile.profileImage} alt={`${profile.name} 프로필 사진`} class="profile__image" width="100" height="100" />
        )}
      </div>

      <p class="profile__intro">{profile.introduction}</p>

      {profile.highlights && profile.highlights.length > 0 && (
        <ul class="profile__highlights">
          {profile.highlights.map((item) => (
            <li>{item}</li>
          ))}
        </ul>
      )}
    </section>

    {/* ── 기술스택 ── */}
    {skills.length > 0 && (
      <ResumeSection title="기술">
        <div class="skills">
          {skills.map((skill) => (
            <SkillTag label={skill} />
          ))}
        </div>
      </ResumeSection>
    )}

    {/* ── 경력 ── */}
    {experience.length > 0 && (
      <ResumeSection title="경력">
        {experience.map((exp) => (
          <div class="exp">
            <div class="exp__header">
              <div class="exp__info">
                {exp.logo && (
                  <img src={exp.logo} alt={`${exp.company} 로고`} class="exp__logo" width="36" height="36" />
                )}
                <div>
                  <h3 class="exp__company">{exp.company}</h3>
                  <p class="exp__role">{exp.role}{exp.department && ` · ${exp.department}`}</p>
                </div>
              </div>
              <div class="exp__meta">
                <span class="exp__period">{exp.period}</span>
                <span class="exp__sub">{exp.duration}{exp.employmentType && ` · ${exp.employmentType}`}</span>
              </div>
            </div>

            {exp.description && (
              <p class="exp__desc">{exp.description}</p>
            )}

            {exp.projects.map((proj) => (
              <article class:list={['project', proj.highlighted && 'project--highlighted']}>
                <div class="project__header">
                  <h4 class="project__name">
                    {typeof proj.name !== 'string' ? (
                      (() => {
                        const split = splitLinkedText(proj.name.text, proj.name.link.label);

                        return split ? (
                          <>
                            {split.before}
                            <a href={proj.name.link.url} target="_blank" rel="noopener noreferrer">{proj.name.link.label}</a>
                            {split.after}
                          </>
                        ) : (
                          proj.name.text
                        );
                      })()
                    ) : proj.url ? (
                      <a href={proj.url} target="_blank" rel="noopener noreferrer">{proj.name}</a>
                    ) : (
                      proj.name
                    )}
                  </h4>
                </div>

                {(proj.summary || proj.description || proj.team || proj.contribution || (proj.techStack && proj.techStack.length > 0)) && (
                  <dl class="project__facts">
                    {(proj.summary || proj.description) && (
                      <div class="project__fact">
                        <dt class="project__fact-label">프로젝트 설명</dt>
                        <dd class="project__fact-text">
                          {proj.summary || proj.description}
                        </dd>
                      </div>
                    )}

                    {proj.team && (
                      <div class="project__fact">
                        <dt class="project__fact-label">팀 구성</dt>
                        <dd class="project__fact-text">{proj.team}</dd>
                      </div>
                    )}

                    {proj.contribution && (
                      <div class="project__fact">
                        <dt class="project__fact-label">기여도</dt>
                        <dd class="project__fact-text">{proj.contribution}</dd>
                      </div>
                    )}

                    {proj.techStack && proj.techStack.length > 0 && (
                      <div class="project__fact">
                        <dt class="project__fact-label">기술</dt>
                        <dd class="project__fact-text project__fact-tech">
                          {proj.techStack.map((tech) => (
                            <SkillTag label={tech} />
                          ))}
                        </dd>
                      </div>
                    )}
                  </dl>
                )}

                {proj.highlighted ? (
                  proj.issues && proj.issues.length > 0 && (
                  <div class="project__narratives">
                    {proj.issues.map((block, index) => (
                      <section class="project__narrative">
                        <h5 class="project__narrative-index">이슈 {index + 1}</h5>
                        <div class="project__narrative-rows">
                          {block.problem && (
                            <div class="project__narrative-row">
                              <div class="project__narrative-label">문제정의</div>
                              <p class="project__narrative-content">{block.problem}</p>
                            </div>
                          )}

                          {block.strategy && (
                            <div class="project__narrative-row">
                              <div class="project__narrative-label">해결전략</div>
                              <p class="project__narrative-content">{block.strategy}</p>
                            </div>
                          )}

                          {block.execution && (
                            <div class="project__narrative-row">
                              <div class="project__narrative-label">주요 실행</div>
                              <p class="project__narrative-content">
                                {typeof block.execution === 'string'
                                  ? block.execution
                                  : (
                                      (() => {
                                        const split = block.execution.link
                                          ? splitLinkedText(block.execution.text, block.execution.link.label)
                                          : null;

                                        return split && block.execution.link ? (
                                          <>
                                            {split.before}
                                            <a
                                              href={block.execution.link.url}
                                              target="_blank"
                                              rel="noopener noreferrer"
                                              class="project__inline-link"
                                            >
                                              {block.execution.link.label}
                                            </a>
                                            {split.after}
                                          </>
                                        ) : (
                                          block.execution.text
                                        );
                                      })()
                                    )}
                              </p>
                            </div>
                          )}

                          {block.impact && (
                            <div class="project__narrative-row">
                              <div class="project__narrative-label">성과</div>
                              <p class="project__narrative-content">{block.impact}</p>
                            </div>
                          )}

                          {block.retrospective && (
                            <div class="project__narrative-row">
                              <div class="project__narrative-label">회고</div>
                              <p class="project__narrative-content project__narrative-content--retrospective">{block.retrospective}</p>
                            </div>
                          )}
                        </div>
                      </section>
                    ))}

                  </div>
                  )
                ) : (
                  proj.details && proj.details.length > 0 && (
                    <ul class="project__details">
                      {proj.details.map((detail) => (
                        <li>
                          {detail.text}
                          {detail.subItems && detail.subItems.length > 0 && (
                            <ul class="project__sub-details">
                              {detail.subItems.map((sub) => (
                                <li>{sub}</li>
                              ))}
                            </ul>
                          )}
                        </li>
                      ))}
                    </ul>
                  )
                )}
              </article>
            ))}
          </div>
        ))}
      </ResumeSection>
    )}

    {/* ── 자격증 ── */}
    {certifications.length > 0 && (
      <ResumeSection title="자격증">
        {certifications.map((cert) => (
          <div class="row-item">
            <div class="row-item__left">
              <span class="row-item__name">{cert.name}</span>
              <span class="row-item__sub">{cert.organization}</span>
            </div>
            <span class="row-item__date">{cert.date}</span>
          </div>
        ))}
      </ResumeSection>
    )}

    {/* ── 학력 ── */}
    {education.length > 0 && (
      <ResumeSection title="학력">
        {education.map((edu) => (
          <div class="row-item">
            <div class="row-item__left">
              <span class="row-item__name">{edu.institution}</span>
              {edu.major && <span class="row-item__sub">{edu.major}{edu.degree && ` (${edu.degree})`}</span>}
            </div>
            <span class="row-item__date">{edu.period}</span>
          </div>
        ))}
      </ResumeSection>
    )}

    {/* ── 활동 ── */}
    {activities.length > 0 && (
      <ResumeSection title="활동">
        {activities.map((act) => (
          <div class="activity">
            <div class="activity__header">
              <div>
                <h3 class="activity__name">{act.name}</h3>
                <p class="activity__org">{act.organization}</p>
              </div>
              <span class="activity__period">{act.period}</span>
            </div>

            {act.description && (
              <p class="activity__desc">{act.description}</p>
            )}

            {act.details && act.details.length > 0 && (
              <ul class="activity__details">
                {act.details.map((detail) => (
                  <li>{detail}</li>
                ))}
              </ul>
            )}

            {act.link && (
              <a href={act.link.url} target="_blank" rel="noopener noreferrer" class="activity__link">
                {act.link.label}
              </a>
            )}
          </div>
        ))}
      </ResumeSection>
    )}
  </main>
</BaseLayout>

<style>
  /* ── 레이아웃 ── */
  .resume {
    padding-top: var(--space-2xl);
    padding-bottom: var(--space-3xl);
  }

  /* ── 프로필 ── */
  .profile {
    margin-bottom: var(--space-2xl);
  }

  .profile__top {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: var(--space-lg);
  }

  .profile__text {
    flex: 1;
    min-width: 0;
  }

  .profile__image {
    width: 100px;
    height: 100px;
    border-radius: 16px;
    object-fit: cover;
    flex-shrink: 0;
  }

  .profile__name {
    font-size: 1.95rem;
    font-weight: 700;
    letter-spacing: var(--tracking-tight);
    line-height: 1.28;
  }

  .profile__role {
    font-size: 1.05rem;
    color: var(--color-text-secondary);
    line-height: 1.6;
    margin-top: var(--space-xs);
  }

  .profile__contact {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-md);
    margin-top: var(--space-md);
    font-size: 0.875rem;
  }

  .profile__contact a {
    color: var(--color-text-secondary);
    text-decoration: none;
  }

  .profile__contact a:hover {
    color: var(--color-accent);
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .profile__intro {
    margin-top: var(--space-lg);
    color: var(--color-text-secondary);
    font-size: 0.95rem;
    line-height: 1.85;
  }

  .profile__highlights {
    margin-top: var(--space-sm);
    padding-left: var(--space-lg);
    color: var(--color-text-secondary);
    line-height: 1.85;
    font-size: 0.91rem;
  }

  .profile__highlights li {
    list-style: disc;
  }

  /* ── 기술스택 ── */
  .skills {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-xs);
  }

  /* ── 경력 ── */
  .exp {
    margin-bottom: var(--space-xl);
  }

  .exp:last-child {
    margin-bottom: 0;
  }

  .exp__header {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
  }

  .exp__info {
    display: flex;
    align-items: flex-start;
    gap: 12px;
  }

  .exp__logo {
    width: 36px;
    height: 36px;
    border-radius: var(--radius-sm);
    object-fit: contain;
    flex-shrink: 0;
    margin-top: 6px;
  }

  .exp__company {
    font-size: 1.16rem;
    font-weight: 700;
    line-height: 1.35;
    letter-spacing: var(--tracking-heading);
  }

  .exp__role {
    font-size: 0.9rem;
    color: var(--color-text-secondary);
  }

  .exp__meta {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .exp__period {
    font-size: 0.8125rem;
    color: var(--color-text-tertiary);
  }

  .exp__sub {
    font-size: 0.8125rem;
    color: var(--color-text-tertiary);
  }

  .exp__desc {
    margin-top: var(--space-sm);
    color: var(--color-text-secondary);
    font-size: 0.91rem;
    line-height: 1.75;
  }

  @media (min-width: 640px) {
    .exp__header {
      flex-direction: row;
      justify-content: space-between;
      align-items: flex-start;
    }

    .exp__meta {
      text-align: right;
      flex-shrink: 0;
    }
  }

  /* ── 프로젝트 ── */
  .project {
    margin-top: var(--space-md);
    padding-left: var(--space-md);
    border-left: 3px solid var(--color-accent);
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .project__header {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .project__name {
    font-size: 1.125rem;
    font-weight: 600;
    line-height: 1.35;
    letter-spacing: var(--tracking-heading);
    color: var(--color-text);
  }

  .project__name a {
    color: inherit;
    text-decoration: none;
  }

  .project__name a:hover {
    color: var(--color-accent);
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .project__facts {
    display: grid;
    gap: var(--space-sm);
  }

  .project__fact {
    display: grid;
    grid-template-columns: 4.5rem minmax(0, 1fr);
    gap: var(--space-sm);
    align-items: start;
    margin: 0;
  }

  .project__fact + .project__fact {
    padding-top: var(--space-sm);
    border-top: 1px dashed var(--color-border);
  }

  .project__fact-label {
    font-size: 0.75rem;
    font-weight: 700;
    color: var(--color-text-tertiary);
    letter-spacing: 0.02em;
    line-height: 1.7;
    margin-top: 1px;
  }

  .project__fact-text {
    margin: 0;
    font-size: 0.875rem;
    color: var(--color-text-secondary);
    line-height: 1.7;
  }

  .project__narratives {
    margin-top: var(--space-sm);
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
  }

  .project__narrative {
    padding-top: var(--space-md);
    border-top: 1px solid var(--color-border);
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .project__narrative-index {
    font-size: 0.8125rem;
    font-weight: 700;
    color: var(--color-accent);
    letter-spacing: 0.02em;
    line-height: 1.7;
  }

  .project__narrative-rows {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .project__narrative-row {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2px;
  }

  .project__narrative-row + .project__narrative-row {
    padding-top: var(--space-xs);
  }

  .project__narrative-label {
    margin-top: 2px;
    font-size: 0.75rem;
    font-weight: 700;
    color: var(--color-text-tertiary);
    letter-spacing: 0.02em;
    line-height: 2;
  }

  .project__narrative-content {
    margin: 0;
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
    line-height: 1.8;
  }

  .project__narrative-content--retrospective {
    font-size: 0.91rem;
    color: var(--color-text-tertiary);
  }

  .project__section {
    padding-top: var(--space-sm);
    border-top: 1px solid var(--color-border);
  }

  .project__section-title {
    font-size: 0.8125rem;
    font-weight: 700;
    color: var(--color-text-tertiary);
    letter-spacing: 0.02em;
  }

  .project__section-list {
    margin-top: 6px;
    padding-left: var(--space-lg);
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
    line-height: 1.8;
  }

  .project__section-list li,
  .project__details > li {
    list-style: disc;
  }

  .project__details {
    padding-left: var(--space-lg);
    font-size: 0.91rem;
    color: var(--color-text-secondary);
    line-height: 1.85;
  }

  .project__sub-details {
    margin-top: 2px;
    padding-left: var(--space-lg);
    font-size: 0.84rem;
    line-height: 1.8;
  }

  .project__sub-details li {
    list-style: circle;
  }

  @media (min-width: 640px) {
    .project__narrative-row {
      grid-template-columns: 4.5rem minmax(0, 1fr);
      gap: var(--space-sm);
      align-items: start;
    }
  }

  .project__fact-tech {
    display: flex;
    flex-wrap: wrap;
    column-gap: var(--space-xs);
    row-gap: 6px;
    align-items: center;
  }

  /* ── 활동 ── */
  .activity {
    margin-bottom: var(--space-md);
    padding-bottom: var(--space-md);
    border-bottom: 1px solid var(--color-border);
  }

  .activity:last-child {
    border-bottom: none;
    margin-bottom: 0;
    padding-bottom: 0;
  }

  .activity__header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: var(--space-md);
  }

  .activity__name {
    font-size: 1rem;
    font-weight: 600;
  }

  .activity__org {
    font-size: 0.875rem;
    color: var(--color-text-secondary);
    margin-top: 2px;
  }

  .activity__period {
    font-size: 0.8125rem;
    color: var(--color-text-tertiary);
    white-space: nowrap;
    flex-shrink: 0;
  }

  .activity__desc {
    margin-top: var(--space-xs);
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
  }

  .activity__details {
    margin-top: var(--space-xs);
    padding-left: var(--space-lg);
    font-size: 0.875rem;
    color: var(--color-text-secondary);
    line-height: 1.8;
  }

  .activity__details li {
    list-style: disc;
  }

  .activity__link,
  .project__inline-link {
    display: inline-block;
    color: var(--color-accent);
    text-decoration: none;
  }

  .activity__link {
    margin-top: var(--space-xs);
    font-size: 0.875rem;
  }

  .activity__link:hover,
  .project__inline-link:hover {
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .project__name a::after,
  .activity__link::after,
  .project__inline-link::after {
    content: '↗';
    display: inline-block;
    margin-left: 2px;
  }

  /* ── 공통 Row 아이템 (자격증, 학력) ── */
  .row-item {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    padding-bottom: var(--space-md);
    border-bottom: 1px solid var(--color-border);
    gap: var(--space-md);
  }

  .row-item:last-child {
    border-bottom: none;
    padding-bottom: 0;
  }

  .row-item__left {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .row-item__name {
    font-size: 1rem;
    font-weight: 600;
  }

  .row-item__sub {
    font-size: 0.875rem;
    color: var(--color-text-secondary);
  }

  .row-item__desc {
    font-size: 0.875rem;
    color: var(--color-text-tertiary);
    margin-top: 2px;
  }

  .row-item__date {
    font-size: 0.8125rem;
    color: var(--color-text-tertiary);
    white-space: nowrap;
    flex-shrink: 0;
  }

</style>
