interface:
  display_name: "Resume Project Story"
  short_description: "Edit this repo's resume copy and layout."
  default_prompt: "Use $resume-project-story to update resume copy, issue narratives, and layout conventions in this repository."

policy:
  allow_implicit_invocation: true
